# Pandas Practice – Data Selection & Filtering 📊

This repository section contains my practice while learning the **Pandas library in Python**.


# 📄 basic.py

This file demonstrates the **core concepts of filtering data in a DataFrame**.

Concepts practiced:

* Filtering rows based on a column value
* Applying conditions on numerical columns
* Filtering data using multiple conditions

Examples covered:

* Filter players with **Runs greater than a certain value**
* Filter players based on **Age**
* Filter rows where **City matches a specific value**
* Apply **multiple conditions using AND (`&`)**
* Apply **multiple conditions using OR (`|`)**

Purpose:
To understand how **boolean indexing works in pandas**.

---

# 📄 practice.py

This file contains **practice tasks from easy to harder level** to strengthen pandas skills.

Concepts practiced:

### Data Selection

* Selecting a **single column**
* Selecting **multiple columns**
* Accessing rows using **`loc`**
* Accessing rows using **`iloc`**
* Selecting a **range of rows**

### Data Filtering

* Filtering rows where **Runs > value**
* Filtering rows where **Age < value**
* Applying **multiple conditions with AND**
* Applying **multiple conditions with OR**

### Combined Selection + Filtering

* Selecting specific columns after filtering

### Creating a New Column

* Creating a calculated column using existing columns
* Example concept: **Runs divided by Matches to create a rate column**

---

# 🧠 Concepts Learned

From these exercises I learned:

* DataFrame column selection
* Row selection using `loc` and `iloc`
* Boolean filtering
* Multiple condition filtering
* Column selection after filtering
* Creating a new column using calculations

---

📌 This repository documents my **step-by-step journey learning pandas and data analysis in Python**.
